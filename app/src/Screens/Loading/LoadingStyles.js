import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  center: {
    flex: 1,
    alignContent: "center",
    justifyContent: "space-around",
    backgroundColor: "#f2c8c9",
    opacity: 0.9,
  },
});

export default styles;
